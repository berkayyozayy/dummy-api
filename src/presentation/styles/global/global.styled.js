import styled from "styled-components";
import * as colors from "presentation/styles/global/colors.styled";

const Text = styled.p({
  fontSize: "1em",
  color: `${colors.indigo}`,
});

export { Text };
